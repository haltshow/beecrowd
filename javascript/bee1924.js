const input = require("fs").readFileSync("stdin", "utf-8");
const lines = input.split("\n");

const rep = lines.shift()

for (let i = 0; i < rep; i++) { 
    let mat = lines.shift()
}

console.log("Ciencia da Computacao")